enable_interfaces: false    # Enable configured interfaces

linux_netconf_simulate: false  # Don't enable modules and configure interfaces (for testing in docker)

rhbase_netconf_path: "/etc/sysconfig/network-scripts"
